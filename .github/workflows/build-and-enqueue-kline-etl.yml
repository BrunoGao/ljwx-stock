name: build-and-enqueue-kline-etl

on:
  push:
    branches: [master]
    paths:
      - 'stock_etl/**'
      - '.github/workflows/build-and-enqueue-kline-etl.yml'
  workflow_dispatch:
    inputs:
      target_env:
        description: 'deploy environment'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev

permissions:
  contents: read
  packages: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v4
      - name: Run stock_etl tests
        run: |
          uv run --with-requirements stock_etl/requirements.txt python -m pytest -q stock_etl/tests

  build:
    if: github.event_name != 'pull_request'
    needs: [test]
    uses: BrunoGao/ljwx-stock/.github/workflows/build-ghcr.yml@master
    with:
      image_name: ljwx-stock-kline-etl
      context: stock_etl
      dockerfile: stock_etl/Dockerfile
      platforms: linux/amd64
      push_latest: false
    secrets: inherit

  enqueue:
    if: github.event_name != 'pull_request'
    needs: [build]
    uses: BrunoGao/ljwx-stock/.github/workflows/enqueue-release.yml@master
    with:
      service: ljwx-stock-kline-etl
      env: dev
      ghcr_ref: ${{ needs.build.outputs.ghcr_ref }}
      tag: ${{ needs.build.outputs.tag }}
      source_git_sha: ${{ github.sha }}
      deploy_repo: BrunoGaoSZ/ljwx-deploy
      deploy_ref: main
      queue_file: release/queue.yaml
      open_pr: true
      auto_merge_pr: true
      merge_method: merge
      pr_title_prefix: queue
    secrets:
      deploy_repo_token: ${{ secrets.DEPLOY_REPO_TOKEN }}

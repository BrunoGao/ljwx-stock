apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: stock-agent
spec:
  rules:
    - host: stock-agent.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: stock-agent
                port:
                  number: 80
